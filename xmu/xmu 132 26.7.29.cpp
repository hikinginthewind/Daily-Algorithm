/*
线性DP·编辑之距
描述

藏剑阁的书架上，赵晴儿发现了两卷残缺的剑谱——剑谱 A 长 N 字符，剑谱 B 长 M 字符。守阁长老言道：两卷剑谱虽非同一版本，但其中隐藏着共同的武学精髓——即最长公共子序列（LCS）。试炼的要求是：找出这个最长公共子序列的长度。

「LCS——双序列 DP 的经典之作，」李少白将两卷剑谱并排展开，「设 dp[i][j] 表示 A 的前 i 个字符与 B 的前 j 个字符的最长公共子序列长度。转移分两种情况。」

「若 A[i] == B[j]，则 dp[i][j] = dp[i-1][j-1] + 1，」赵晴儿执笔演算，「两字符相同，自然可以接在前面的公共子序列之后。若不同，则 dp[i][j] = max(dp[i-1][j], dp[i][j-1])——要么跳过 A 的这个字符，要么跳过 B 的这个字符。」

「O(N*M) 填完整个二维表，」梁嘉峰指着渐次被填满的矩阵，「dp[N][M] 便是答案。空间可优化至 O(min(N,M))——因为每行只依赖上一行和当前行的前缀。」

「两卷剑谱，一脉共序，」李少白收剑，「DP 表中每一个数字，都是武学传承的见证。」


输入

第一行包含两个整数N和M。

第二行包含一个长度为N的字符串，表示字符串A。

第三行包含一个长度为M的字符串，表示字符串B。

字符串均由小写字母构成。


输出

输出一个整数，表示最大长度。


输入样例 1 

4 5
acbd
abedc
输出样例 1

3
提示

原题链接

本题改编自AcWing 902. 最短编辑距离、AcWing 899. 编辑距离
董晓讲解: B站视频 — E07 线性DP 编辑距离

来源

xmu
*/

/*
dp
对于每一个尾巴 相同或不同
相同则更新 dp 为上一 dp++
不同则取 a 和 b 分别去掉末尾后 更大的前 dp 
*/

#include <bits/stdc++.h>
using namespace std;
int main()
{
	int n,m;
	cin >> n >> m;
	string a,b;
	cin >> a >> b;
	a = " " + a;
	b = " " + b;
	vector<vector<int>> dp(n + 1,vector<int>(m + 1,0));
	for(int i = 1;i <= n;i++)
	{
		for(int j = 1;j <= m;j++)
		{
			if(a[i] == b[j]) dp[i][j] = dp[i - 1][j - 1] + 1;
			else dp[i][j] = max(dp[i - 1][j],dp[i][j - 1]);
		}	
	}
	cout << dp[n][m] << endl; 
	return 0;	
} 
